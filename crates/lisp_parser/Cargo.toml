[package]
name = "lisp_parser"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lisp parser for the pwn_arena workspace"

[dependencies]
pwn_arena = { path = "../pwn_arena" }
lisp_macros = { path = "../lisp_macros" }
